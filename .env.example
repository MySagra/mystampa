# Example environment configuration for the Mycassa application.
#
# Copy this file to `.env` in the project root and adjust the values
# according to your environment. Variables defined here control how the
# application connects to external services and where it listens locally.

# Base URL of the external API. This should include the protocol and port.
EXTERNAL_BASE_URL=http://localhost:4300

# Credentials used to authenticate against the external service.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Port where this application should listen. Defaults to 1234 if not set.
PORT=1234