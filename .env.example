# Configuration for the mystampa service

# Base URL of the external API used for login and data fetching
EXTERNAL_BASE_URL=http://localhost:4300

# Credentials for authenticating against the external API
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Local port for this service
PORT=1234

# Mode selection: set USE_SSE to 'true' or 'sse' to enable the
# Server‑Sent Events client. When enabled the service connects to
# the SSE_URL endpoint and forwards received messages to the local
# /print route. When disabled the service exposes the REST
# endpoint /print and relies on clients to POST orders directly.
USE_SSE=false

# URL of the SSE endpoint to connect to when USE_SSE is enabled
# Example: http://localhost:3001/events/cashier
SSE_URL=http://localhost:3001/events/cashier